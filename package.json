{
  "scripts": {
    "dev": "vite",
    "build": "echo 'Using pre-built dist folder'",
    "lint": "eslint .",
    "preview": "vite preview", 
    "serve": "serve dist",
    "start": "serve -s dist -p $PORT"
  }
}
